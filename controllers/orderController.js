const { Association } = require('sequelize');
const db = require('../../models/index');


const orderController = {

    order: (req,res)=>{
        //Necesito en json id de usuario - total del pedido - Descuentos aplicados -
        // Crear un envio y asociarlo (opcional) - crear un pago y asociarlo- 
        // Crear detalles pedidos segun el array de productos y cantidad que me pasen del front
        //recorrer clave valor id producto cantidad  
    }
}